# Copy to secrets.env and export before running runtime-flight commands.
# Secrets exist only in environment variables and never belong in YAML.

RUNTIME_BASELINE_ID=
TEXT_BASE_URL=
TEXT_API_KEY=
# Optional fallback. Prefer text.model in config yaml. The slug is not a secret.
TEXT_MODEL=
RUNTIME_SPEND_CAP_USD=
OBS_WEBSOCKET_PASSWORD=
FAL_KEY=
