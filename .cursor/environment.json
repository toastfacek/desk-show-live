{
  "name": "Desk Show Runtime",
  "install": "sudo apt-get update && sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends ffmpeg python3-venv python3-pip && python3 -m venv .venv && . .venv/bin/activate && python -m pip install --upgrade pip && pip install -e './pack-manager' -e './obs-harness' -e './runtime-flight[dev]' && chmod +x scripts/stage-demo.sh && ./scripts/stage-demo.sh"
}
